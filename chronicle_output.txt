 Restyle the Dashboard Overview to match the attached mockup

  ## Goal
  Make the **Overview page** match the attached design mockup (screenshot
  included
  in this message). This is a **visual/layout** change only — the data, links,
  icons, and logic are already correct and must be preserved.

  ## The one file to edit
  `app/dashboard/page.js` — this is the entire Overview page (a Next.js **async
  server component**). Full current source is at the bottom of this handoff.

  Do NOT touch anything else. In particular, the left sidebar lives in
  `components/dashboard/DashboardShell.jsx` and is out of scope.

  ## What you MAY change
  - The JSX layout / markup of the header and the module card grid.
  - Tailwind classes, spacing, card styling, hover states, grid columns.
  - Add lucide-react icon imports if the design needs new ones.
  - The shared card primitives are in `components/ui/card.jsx` (shadcn). You can
    use them or hand-roll markup — match the mockup.

  ## What you must NOT change (hard constraints)
  1. **Keep the `MODULES` array exactly as-is** — same titles, descriptions,
     `href`s, and icons. Order: Scheduling, Area Pulse, End of Night,
     PR & Socials, SIO.Proof, Staff.
  2. **Keep the PR→Reviews swap.** The render must keep using the computed
     `modules` variable (NOT `MODULES`), which swaps the "PR & Socials" card for
     `REVIEWS_MODULE` when `venue?.social_media_enabled` is false. Don't
  hardcode.
  3. **Keep it a server component**: the `async function`, the `getVenueUser()`
     call, and the `venueUser.role === "staff"` redirect must stay.
  4. **Keep each card a whole-card link**: the `<Link>` wraps the card so the
     entire card is the click target (the "Open →" affordance is a plain
     `aria-hidden` span, not a separate button). Preserve this a11y pattern and
     the `focus-visible` ring.
  5. **No "Sidework" card.** Sidework is intentionally sidebar-only.

  ## Design system / theme (for matching the rest of the app)
  - Tailwind tokens (defined in `app/globals.css`): `bg-background` (warm
  cream),
    `text-foreground`, `text-muted-foreground`, `border-border`, `bg-accent`,
    `bg-primary` / `text-primary` (burnt orange), `ring-ring`.
  - Eyebrow labels use: `font-mono text-[11px] uppercase tracking-[0.22em]
    text-muted-foreground`.
  - Headings: serif/large, e.g. the venue name uses
    `text-5xl font-semibold tracking-tight`.
  - Page is wrapped in `mx-auto w-full max-w-6xl px-6 py-12 ... lg:px-10`.

  ## How to preview
  2. Log in, then go to `/dashboard` (the Overview).
     (Note: `/dashboard` redirects staff-role users to `/dashboard/schedule`, so
     use a manager/owner account.) 
     
  ## Deliverable
  Updated `app/dashboard/page.js` that visually matches the mockup, with all
  five
  constraints above intact. Keep the diff scoped to this one file.
  
  ---
  
  ## Current source of `app/dashboard/page.js`
